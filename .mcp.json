{
  "mcpServers": {
    "lean-lsp-mcp": {
      "command": "lean-lsp-mcp",
      "args": [
        "--transport", "stdio",
        "--lean-project-path", "./hax-lib/proof-libs/lean"
      ],
      "env": {
        "PATH": "${HOME}/.elan/bin:${PATH}"
      }
    }
  }
}
